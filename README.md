# benchpub

> Publish benchmark evidence, not just benchmark numbers.

`benchpub` is a local-first CLI for validating benchmark result contracts, checking comparability under declared controls, preserving provenance, and rendering static evidence reports.

The project is being bootstrapped. See the first foundation pull request for the initial product skeleton.
